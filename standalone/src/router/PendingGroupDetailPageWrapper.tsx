/**
 * Wrapper pour PendingGroupDetailPage
 * Charge la page de détails d'un groupe en attente
 */

import { PendingGroupDetailPage } from '../components/PendingGroupDetailPage';

export function PendingGroupDetailPageWrapper() {
  return <PendingGroupDetailPage />;
}
