import { GroupsExplorerPage } from "../components/GroupsExplorerPage";

/**
 * Wrapper pour la page d'exploration des groupes
 * Phase 1 : Annuaire des groupes
 */
export function GroupsExplorerPageWrapper() {
  return <GroupsExplorerPage />;
}
