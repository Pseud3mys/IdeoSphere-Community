#VITE_API_BASE_URL=http://localhost:5000/api
VITE_API_BASE_URL=/api

# --- CONFIG KEYCLOAK (LOCAL) ---
# On pointe vers le localhost via le port que nous avons ouvert dans le docker-compose.override.yml
VITE_KEYCLOAK_URL=http://localhost:8080
VITE_KEYCLOAK_REALM=ideosphere
VITE_KEYCLOAK_CLIENT_ID=ideosphere-front